import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 30,
    // all below is just for test
    alignItems: 'center',
    justifyContent: 'center',
  },
});
